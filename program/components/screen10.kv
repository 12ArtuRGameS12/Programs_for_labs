<Screen10@Screen>:
    name: "Статистика guide"
    BoxLayout:
        orientation: "vertical"
        padding: (50, 20)
        spacing: 5
        Button:
            text: "Назад"
            size_hint: (1, 0.2)
            on_press:
                app.root.current = app.last
        MyLabel:
            text: open("program/text/Статистика guide.txt", encoding="utf-8").read()
